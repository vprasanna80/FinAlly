services:
  finally:
    build: .
    image: finally:latest
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  finally-data:
